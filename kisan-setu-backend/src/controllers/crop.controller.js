import {prisma} from '../config/database.js'; import {errors} from '../utils/errors.js'; import {success} from '../utils/response.js'; async function owner(id){const p=await prisma.farmerProfile.findUnique({where:{userId:id}});if(!p)throw errors.forbidden('Farmer profile required');return p.id} export async function create(req,res){const id=await owner(req.user.id);return success(res,await prisma.crop.create({data:{farmerId:id,...req.body}}),'Crop created',201)} export async function list(req,res){const id=await owner(req.user.id);return success(res,await prisma.crop.findMany({where:{farmerId:id,deletedAt:null},orderBy:{createdAt:'desc'}}))} export async function getOne(req,res){const id=await owner(req.user.id);const x=await prisma.crop.findFirst({where:{id:req.params.id,farmerId:id,deletedAt:null}});if(!x)throw errors.notFound('Crop not found');return success(res,x)} export async function update(req,res){const id=await owner(req.user.id);const x=await prisma.crop.findFirst({where:{id:req.params.id,farmerId:id,deletedAt:null}});if(!x)throw errors.notFound('Crop not found');return success(res,await prisma.crop.update({where:{id:x.id},data:req.body}),'Crop updated')} export async function remove(req,res){const id=await owner(req.user.id);const x=await prisma.crop.findFirst({where:{id:req.params.id,farmerId:id,deletedAt:null}});if(!x)throw errors.notFound('Crop not found');await prisma.crop.update({where:{id:x.id},data:{deletedAt:new Date()}});return res.status(204).send()}
