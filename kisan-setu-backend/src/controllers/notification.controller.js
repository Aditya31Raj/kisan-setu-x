import {prisma} from '../config/database.js'; import {errors} from '../utils/errors.js'; import {success} from '../utils/response.js'; export const list=async(req,res)=>success(res,await prisma.notification.findMany({where:{userId:req.user.id},orderBy:{createdAt:'desc'},take:100})); export async function markRead(req,res){const n=await prisma.notification.findFirst({where:{id:req.params.id,userId:req.user.id}});if(!n)throw errors.notFound('Notification not found');return success(res,await prisma.notification.update({where:{id:n.id},data:{readAt:new Date()}}),'Notification marked read')}
