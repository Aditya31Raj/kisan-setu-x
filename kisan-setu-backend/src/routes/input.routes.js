import{Router}from'express';import * as c from'../controllers/input.controller.js';import{authenticate}from'../middleware/authenticate.js';import{authorize}from'../middleware/authorize.js';import{validate}from'../middleware/validate.js';import{inputSchema,inputReviewSchema,inputProductSchema}from'../validators/input.schema.js';import{csrfProtection}from'../middleware/csrf.js';const r=Router();r.use(authenticate);r.post('/',authorize('FARMER'),csrfProtection,validate(inputSchema),c.create);r.get('/',authorize('FARMER','PRAKHAND_ADMIN','SUPER_ADMIN'),c.list);r.get('/:id',authorize('FARMER','PRAKHAND_ADMIN','SUPER_ADMIN'),c.getOne);r.post('/:id/approve',authorize('PRAKHAND_ADMIN','SUPER_ADMIN'),csrfProtection,validate(inputReviewSchema),c.approve);r.post('/:id/reject',authorize('PRAKHAND_ADMIN','SUPER_ADMIN'),csrfProtection,validate(inputReviewSchema),c.reject);r.post('/products',authorize('PRAKHAND_ADMIN','SUPER_ADMIN'),csrfProtection,validate(inputProductSchema),c.createProduct);export default r;
