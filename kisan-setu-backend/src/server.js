import {app} from './app.js'; import {env} from './config/env.js'; import {connectDatabase,disconnectDatabase} from './config/database.js'; import {connectRedis} from './config/redis.js'; import {logger} from './config/logger.js'; await connectDatabase(); await connectRedis(); const server=app.listen(env.PORT,()=>logger.info({port:env.PORT},'Kisan Setu backend started')); async function shutdown(signal){logger.info({signal},'Shutting down');server.close(async()=>{await disconnectDatabase();process.exit(0)})} process.on('SIGTERM',()=>shutdown('SIGTERM'));process.on('SIGINT',()=>shutdown('SIGINT'));
