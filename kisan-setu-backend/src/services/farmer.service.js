import {prisma} from '../config/database.js'; import {errors} from '../utils/errors.js'; export const profile=id=>prisma.farmerProfile.findUnique({where:{userId:id},include:{crops:{where:{deletedAt:null}}}}).then(x=>x||Promise.reject(errors.notFound('Farmer profile not found'))); export const update=(id,d)=>prisma.farmerProfile.update({where:{userId:id},data:d}); export async function dashboard(id){const[a,b,c,d,e,f]=await Promise.all([prisma.produceListing.aggregate({where:{farmerId:id,deletedAt:null},_count:true,_sum:{availableQuantity:true,soldQuantity:true}}),prisma.order.count({where:{farmerId:id,status:{notIn:['COMPLETED','CANCELLED','REJECTED']}}}),prisma.payment.aggregate({where:{order:{farmerId:id},status:'SUCCESS'},_sum:{amount:true}}),prisma.payment.count({where:{order:{farmerId:id},status:'PENDING'}}),prisma.inputRequest.count({where:{farmerId:id,status:'PENDING'}}),prisma.grievance.count({where:{createdById:id,status:{in:['OPEN','IN_REVIEW']}}})]);return{totalProduceListings:a._count,availableQuantity:a._sum.availableQuantity,soldQuantity:a._sum.soldQuantity,activeOrders:b,totalEarnings:c._sum.amount||0,pendingPayments:d,pendingInputRequests:e,openComplaints:f}}
