import {prisma} from '../config/database.js'; import {NotificationProvider} from '../providers/notification/notificationProvider.js'; const provider=new NotificationProvider(); export async function notifyUser({userId,title,message,type='IN_APP',metadata={}}){const u=await prisma.user.findUnique({where:{id:userId},select:{email:true,phone:true}});if(!u)return null;await provider.send({type,recipient:type==='EMAIL'?u.email:u.phone,title,message});return prisma.notification.create({data:{userId,type,title,message,metadata}})}
