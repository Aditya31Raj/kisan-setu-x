import {prisma} from '../config/database.js'; import {errors} from '../utils/errors.js'; import {parsePagination,paginationMeta} from '../utils/pagination.js'; import {enforceListingCompliance} from './msp.service.js'; import {recordAudit} from './audit.service.js';
export async function create(userId,d,m){const p=await prisma.farmerProfile.findUnique({where:{userId}});if(!p)throw errors.forbidden('Farmer profile required');const c=await prisma.crop.findFirst({where:{id:d.cropId,farmerId:p.id,deletedAt:null}});if(!c)throw errors.notFound('Crop not found or not owned');const x=await enforceListingCompliance({cropId:d.cropId,location:d.location,pricePerKg:d.pricePerUnit,farmerId:userId,requestId:m.requestId,ipAddress:m.ip});if(x.violation)throw errors.unprocessable(`Price is below applicable MSP (${x.mspPrice}/${d.unit})`);const l=await prisma.produceListing.create({data:{farmerId:userId,...d,status:'LISTED'}});await recordAudit({userId,role:'FARMER',action:'PRODUCE_CREATED',entity:'ProduceListing',entityId:l.id,requestId:m.requestId,ipAddress:m.ip});return l}
export async function list(q){const{page,limit,skip}=parsePagination(q);const where={deletedAt:null,status:{in:['LISTED','RESERVED']},...(q.cropId?{cropId:q.cropId}:{}),...(q.location?{location:{contains:q.location,mode:'insensitive'}}:{}),...(q.q?{OR:[{title:{contains:q.q,mode:'insensitive'}},{description:{contains:q.q,mode:'insensitive'}}]}:{}),...(q.minPrice!==undefined||q.maxPrice!==undefined?{pricePerUnit:{...(q.minPrice!==undefined?{gte:q.minPrice}:{}),...(q.maxPrice!==undefined?{lte:q.maxPrice}:{})}}:{}),...(q.availableOnly==='true'?{availableQuantity:{gt:0}}:{})};const[a,total]=await prisma.$transaction([prisma.produceListing.findMany({where,include:{crop:{select:{id:true,name:true,variety:true}},farmer:{select:{id:true,name:true}}},orderBy:{createdAt:'desc'},skip,take:limit}),prisma.produceListing.count({where})]);return{items:a,pagination:paginationMeta(page,limit,total)}}
export async function get(id){const x=await prisma.produceListing.findFirst({where:{id,deletedAt:null},include:{crop:true}});if(!x)throw errors.notFound('Produce listing not found');return x}
export async function update(userId,id,d,m){const old=await prisma.produceListing.findFirst({where:{id,farmerId:userId,deletedAt:null}});if(!old)throw errors.notFound('Produce listing not found');const cropId=d.cropId||old.cropId,loc=d.location||old.location,price=d.pricePerUnit??Number(old.pricePerUnit);const x=await enforceListingCompliance({cropId,location:loc,pricePerKg:price,farmerId:userId,requestId:m.requestId,ipAddress:m.ip});if(x.violation)throw errors.unprocessable(`Price is below applicable MSP (${x.mspPrice})`);return prisma.produceListing.update({where:{id},data:d})}
export async function remove(userId,id){const x=await prisma.produceListing.findFirst({where:{id,farmerId:userId,deletedAt:null}});if(!x)throw errors.notFound('Produce listing not found');if(Number(x.reservedQuantity)>0)throw errors.conflict('Reserved quantity exists');await prisma.produceListing.update({where:{id},data:{status:'DELETED',deletedAt:new Date()}})}
